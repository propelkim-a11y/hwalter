# 활터 왔소 — GitHub·Vercel 배포 안내

이 ZIP은 최신 **활터 왔소** 소스입니다. 시수 일지 검색·기간 선택, 글자 크기·고대비 설정, 기타 안내 건수 요약, 기존 사용자 업데이트 안내 및 40px 삼족오 헤더 마크가 포함되어 있습니다.

## 1. GitHub에 최신 소스 올리기

1. ZIP 압축을 풉니다. 최상위 `hwalter-isseo` 폴더 안의 파일을 기존 GitHub 저장소의 최상위 폴더에 **덮어씁니다**.
2. 터미널에서 저장소 폴더로 이동한 뒤 아래 명령을 차례로 실행합니다.

```bash
git add -A
git commit -m "최신 시수 일지 검색과 접근성 개선 적용"
git push
```

## 2. Vercel 배포 확인

GitHub 저장소가 연결된 Vercel 프로젝트라면 `git push` 뒤 자동으로 새 배포가 시작됩니다. Vercel의 Deployments 화면에서 최신 배포가 `Ready`가 된 뒤, 배포 주소를 새로고침해 확인하세요.

기존 사용자는 화면 아래의 **새 버전 적용**을 누르면 대기 중인 서비스 워커가 활성화됩니다. 홈 화면 아이콘을 이미 추가해 둔 휴대폰에서는 이전 바로가기를 삭제한 뒤, 배포 주소를 Safari 또는 Chrome에서 연 다음 다시 **홈 화면에 추가**하면 최신 녹색 바탕·흰색 삼족오 아이콘을 받을 수 있습니다.

## 포함된 로컬 삼족오 자산

`client/public/`에 아래 파일을 포함했습니다. HTML, 웹 앱 매니페스트 및 헤더는 이 로컬 파일만 참조하므로 Manus 전용 자산 주소에 의존하지 않습니다.

| 파일 | 용도 |
|---|---|
| `samjoko-brand-mark.png` | 사용자 제공 원본 삼족오 헤더 마크 |
| `icon-192.png` | Android 192px 설치 아이콘 |
| `icon-512.png` | Android any·maskable 512px 설치 아이콘 |
| `apple-touch-icon.png` | iOS 홈 화면 아이콘 |

## 배포 전 선택 사항

Vercel에서 프로젝트 환경 변수를 별도로 사용 중이라면 기존 프로젝트에 설정된 값은 유지하세요. 이 ZIP에는 비밀키나 별도 환경 변수 파일을 포함하지 않았습니다.
